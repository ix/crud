#define FOREGROUND_COLOR 0x888888 // color to use for the selection border, RGB
#define BORDER 4                  // width of the selection border, in pixels
#define PERSISTENT_GRABBING 0     // if nonzero, crud will retry grabbing the cursor repeatedly if it initially fails
